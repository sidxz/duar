services:
  identity-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: identity
      POSTGRES_USER: identity
      POSTGRES_PASSWORD: identity_dev
    # Copy TLS certs with correct ownership/permissions, then start with SSL
    entrypoint:
      - sh
      - -c
      - |
        cp /tls/server.crt /tmp/server.crt
        cp /tls/server.key /tmp/server.key
        chown postgres:postgres /tmp/server.crt /tmp/server.key
        chmod 600 /tmp/server.key
        exec docker-entrypoint.sh postgres \
          -c ssl=on \
          -c ssl_cert_file=/tmp/server.crt \
          -c ssl_key_file=/tmp/server.key
    ports:
      - "9001:5432"
    volumes:
      - identity_pg_data:/var/lib/postgresql/data
      - ./keys/tls:/tls:ro
    networks:
      - duar-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U identity"]
      interval: 5s
      timeout: 5s
      retries: 5

  identity-redis:
    image: redis:7-alpine
    command: >
      redis-server
      --requirepass duar_dev
      --tls-port 6379
      --port 0
      --tls-cert-file /tls/server.crt
      --tls-key-file /tls/server.key
      --tls-ca-cert-file /tls/ca.crt
      --tls-auth-clients no
    ports:
      - "9002:6379"
    volumes:
      - ./keys/tls:/tls:ro
    networks:
      - duar-network
    healthcheck:
      test: ["CMD", "redis-cli", "--tls", "--cacert", "/tls/ca.crt", "-a", "duar_dev", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  duar:
    build:
      context: service
    ports:
      - "9003:9003"
    env_file:
      - service/.env
    environment:
      DEBUG: "true"
      DATABASE_URL: "postgresql+asyncpg://identity:identity_dev@identity-postgres:5432/identity?ssl=require"
      REDIS_URL: "rediss://:duar_dev@identity-redis:6379/0"
      REDIS_TLS_CA_CERT: "/keys/tls/ca.crt"
      ALLOWED_HOSTS: "localhost,duar"
      # RATE_LIMIT_RPM removed (GlobalRateLimitMiddleware deleted). Defaults are fine
      # for dev; to disable rate limiting entirely for load testing, set RATE_LIMIT_ENABLED: "false".
    volumes:
      - ./keys:/keys:ro
    depends_on:
      identity-postgres:
        condition: service_healthy
      identity-redis:
        condition: service_healthy
    networks:
      - duar-network

volumes:
  identity_pg_data:

networks:
  duar-network:
    driver: bridge
