name: Publish Admin Image

on:
  push:
    tags:
      - "admin-v*"
  workflow_dispatch:

jobs:
  publish:
    name: Build & Push Admin Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#admin-v}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: admin/
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/duar-admin:${{ steps.version.outputs.VERSION }}
            ghcr.io/${{ github.repository_owner }}/duar-admin:latest
