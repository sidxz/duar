VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_DUAR_URL=http://localhost:9003
# Backend proxy URL — DuarAuthz mints authz tokens via ${VITE_BACKEND_URL}/auth/mint
# (not directly against Duar) so the service key stays server-side.
VITE_BACKEND_URL=http://localhost:9200
